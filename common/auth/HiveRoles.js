
/**
 *
 */
class HiveRoles {
    static get CLIENT() { return `CLIENT`; }
    static get ADMIN() { return `ADMIN`; }
    static get KEY() { return `KEY`; }
    static get JWT() { return `JWT`; }
}


module.exports = HiveRoles;
